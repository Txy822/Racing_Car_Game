package Server;

import org.junit.Before;
import org.junit.Test;

public class ClientHandlerTest {

  private ClientHandler clientHandler;

  @Test
  public void run() {
    //assertTrue(clientHandler.getRequests().get(0).getClass() == InitRequest.class);
  }

  @Before
  public void setUp() throws Exception {
    //Server.main(null);
    //Client client = new Client("loalhost");
    //client.connect();
    //clientHandler = Server.getClients().get(0);
  }
}